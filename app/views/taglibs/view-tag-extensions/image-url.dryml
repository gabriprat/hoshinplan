<def tag='view' for="HoboFields::Types::ImageUrl">
	#{this.to_html}
</def>

<def tag="view" for="Paperclip::Attachment">
  <image param src="#{this.url(:thumb)}" class="#{'no-image' unless this.exists?}" merge-attrs/>
</def>