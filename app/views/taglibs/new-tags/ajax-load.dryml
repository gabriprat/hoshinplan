<def tag="ajax-load" attrs="id, updates">
		<unless test="&params[:ajax_load]">
			<a href="?ajax_load=true" action="show" updates="#{updates}" id="update-#{id}" ajax data-rapid='{"ajax-load":{}}' no-spinner></a>
		</unless>
		<else>
			<do param="content"/>
		</else>
</def>